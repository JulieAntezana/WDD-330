import ToDos from './todo.js';

const list = document.getElementById('todoList');
const myToDos = new ToDos(list, 'todo'); // Constant named myToDos = create a new list and store it under the name "todo"

